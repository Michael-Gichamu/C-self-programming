#include <stdio.h>
#include <stdlib.h>

/**
 * main - for loop
 *
 * Return: Always(0) Success.
 */

int main()
{
     int luckynumber[] = {1, 2, 3, 4, 5, 6};
     for(int i = 0; i <= 5 i++)
     {
         printf("%d\n", luckynumber[i]);
     }
     return (0);
}
